# YOURSITE responsive frontend

This version fixes the navigation structure, mobile drawer, touch-friendly controls, responsive grids, shared footer/header, language selector UI, and light/dark mode.

GitHub Pages can host the frontend. Real accounts, uploads, downloads, and database storage require a backend service.
